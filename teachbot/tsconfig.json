{
    "compilerOptions": {
        "target": "es5",
        "lib": ["dom", "dom.iterable", "esnext"],
        "allowJs": true,
        "skipLibCheck": true,
        "esModuleInterop": true,
        "allowSyntheticDefaultImports": true,
        "strict": true,
        "forceConsistentCasingInFileNames": true,
        "module": "esnext",
        "moduleResolution": "node",
        // JSX 코드가 있는 .tsx 파일 사용을 위해 필요합니다.
        "jsx": "react",
        // 컴파일된 JS 파일이 저장될 디렉토리입니다.
        "outDir": "./dist",
        // TS 소스맵 생성 여부입니다.
        "sourceMap": true
    },
    // TypeScript가 검사할 파일 경로입니다.
    // 모든 .ts와 .tsx 파일을 검사하도록 설정했습니다.
    // exclude 속성으로 제외할 경로를 지정할 수 있습니다.
    // 보통 build나 node_modules 등의 디렉토리를 제외합니다.
    //
    "include": ["./src/**/*.ts", "./src/**/*.tsx"],
    "exclude": ["node_modules"]
}
